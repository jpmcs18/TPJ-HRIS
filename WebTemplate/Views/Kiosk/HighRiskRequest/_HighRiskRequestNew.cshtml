@model WebTemplate.Models.Kiosk.High_Risk_Request.Index

@{
    var c = User as WebTemplate.Models.CustomPrincipal;
    var credential = (Session["CredentialPages"] as DataAccessLayer.Security.CredentialPages);
    var CreatorAccess = credential.GetPage("Creator High Risk Request") ?? new DataAccessLayer.System.Page();
}

@Html.Hidden("PersonnelID", Model.Personnel.ID, new { @class = "form-control" })

@*<div class="form-group col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <label class="control-label">Request Date</label>
    @Html.TextBoxFor(m => Model.HighRiskRequest.RequestDate, String.Format("{0:MM/dd/yyyy}", Model.HighRiskRequest.RequestDate), new { @class = "form-control js-datepicker fix-values", @placeholder = "MM/dd/yyyy" })
    <span id="" class="clearInput" title="Clear"><i class="fa fa-times"></i></span>
</div>*@

<div class="form-group col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <label class="control-label">Request Date</label>
    @Html.TextBox("RequestDate", String.Format("{0:yyyy'-'MM'-'dd}", ""), new { @type = "date", @class = "form-control" })
</div>

<div class="form-group col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <label class="control-label">Reasons</label>
    @Html.TextArea("Reasons", "", new { @class = "form-control", @rows = "4", @placeholder = "Reasons..." })
</div>

@if (CreatorAccess.Update || CreatorAccess.Insert)
{
    <div class="form-group col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <button type="button" id="CreateHighRiskRequest" onclick="SaveRequest();" class="btn btn-primary font-bold pull-right" title="Save"><i class="fa fa-save"></i>&nbsp;&nbsp;Save</button>
    </div>
}